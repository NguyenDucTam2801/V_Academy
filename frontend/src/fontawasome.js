// Import the FontAwesome library manager
import { library } from '@fortawesome/fontawesome-svg-core';

// Import the trash icon and any other icons you need
import { faCode, faHighlighter, faTrash } from '@fortawesome/free-solid-svg-icons';

// Add the icons to the custom library
library.add( faCode, faHighlighter, faTrash);
